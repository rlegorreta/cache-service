audit-service:
  testing: true
eureka:
  client:
    registerWithEureka: false
logging:
  level:
    root: INFO
spring:
  main:
    allow-bean-definition-overriding: true
  data:
    redis:
      database: 0
      password: mypass
      timeout: 60000
      jedis:
        pool:
          enabled: true