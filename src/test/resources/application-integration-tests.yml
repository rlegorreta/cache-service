audit-service:
  testing: true
eureka:
  client:
    enabled: false
    registerWithEureka: false
logging:
  level:
    root: INFO
spring:
  data:
    redis:
      database: 0
      password: mypass
      timeout: 60000
      jedis:
        pool:
          enabled: true